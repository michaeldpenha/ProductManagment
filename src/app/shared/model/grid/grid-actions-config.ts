import {ActionGridItemsConfig} from '@app/shared/components';
export class GridActionsConfig {
    public config : any;
    constructor(cfg : ActionGridItemsConfig){
        this.config = cfg;
    }
}